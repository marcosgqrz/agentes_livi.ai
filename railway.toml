[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn web.app:app --bind 0.0.0.0:8080 --workers 2 --worker-class gthread --threads 4 --timeout 300"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
